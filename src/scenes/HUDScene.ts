import Phaser from 'phaser';

export class HUDScene extends Phaser.Scene {
  constructor() {
    super({ key: 'HUDScene' });
  }

  create(): void {
    // HUD will be implemented in Phase 1
    // For now, this scene is a no-op placeholder
  }
}
